/**
 * Online payments (Razorpay) — flip to `true` when live keys are configured.
 * While `false`: enquiry / callback leads only (no Pay & Book / Buy now).
 */
export const ONLINE_PAYMENT_ENABLED = false;
